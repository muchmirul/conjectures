"""The game: condensed mathematics discovered rather than presented.

`model.py` is the shape of a brick, `worlds/` is the content, `widgets.py` is
the twelve things a reader can move, and `build_game.py` renders the pages.
"""
